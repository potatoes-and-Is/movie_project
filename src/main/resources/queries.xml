<?xml version="1.0" encoding="UTF-8"?>
<!-- 테스트 용 쿼리-->
<queries>
    <query id="getAllUsers">
        SELECT
        *
        FROM users
    </query>

    <query id="getSchedulesBymovieId">
        SELECT
        schedules.schedule_id,
        schedules.schedule_start_time
        FROM schedules
        JOIN movie_infos ON schedules.schedule_id = movie_infos.schedule_id
        WHERE movie_infos.movie_id = ?
    </query>
</queries>